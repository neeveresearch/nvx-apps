// assume launched
assume launchoms has run

// shutdown the oms cluster
stop the oms app in the oms2.${oms.env::local} xvm
stop the oms app in the oms1.${oms.env::local} xvm

// shutdown the xvms
shutdown

