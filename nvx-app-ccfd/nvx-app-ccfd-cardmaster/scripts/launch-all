##DISPLAYNAME Launch All
##DESCRIPTION Launches all XVMs in the system

assume provision has run

// launch the processor cluster 
launch the cardmaster-1 xvm
launch the cardmaster-2 xvm

// Launch the receiver driver
launch the cardmaster-receive-driver xvm

// launch the send driver
launch the cardmaster-send-driver xvm
